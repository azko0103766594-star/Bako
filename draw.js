function draw() {

    ctx.clearRect(
        0,
        0,
        canvas.width,
        canvas.height
    );

    // plateau

    // scores

    // tour actuel

    // zones sombres

    // flèche

    // fin de partie

    requestAnimationFrame(draw);
}
console.log("draw chargé");
